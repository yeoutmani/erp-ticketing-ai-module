services:
  frontend:
    volumes: ["./frontend:/app"]
    command: npm run dev -- -p 3001
  automation:
    volumes:
      - ./automation:/app 
      - /app/node_modules
    command: npx tsx watch server.ts

  n8n:
    ports: ["5678:5678"]
  ollama:
    ports: ["11434:11434"]